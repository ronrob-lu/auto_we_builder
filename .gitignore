(auto_we_builder/.vscode/)
(auto_we_builder/__pycache__/)
(auto_we_builder/build/)
(auto_we_builder/dist/)
(auto_we_builder/node_modules/)
(auto_we_builder/target/)
(auto_we_builder/.gradle/)
(auto_we_builder/venv/)
(auto_we_builder/.venv/)
(auto_we_builder/.env)
(auto_we_builder/*.env.*)
(auto_we_builder/*.log)
(auto_we_builder/*.tmp)
(auto_we_builder/*.swp)
(auto_we_builder/*.pyc)
(auto_we_builder/*.class)
(auto_we_builder/*.o)
(auto_we_builder/*.exe)
(auto_we_builder/*.dll)
(auto_we_builder/*.so)
(auto_we_builder/*.a)
(auto_we_builder/*.obj)
(auto_we_builder/*.out)
(auto_we_builder/.DS_Store)
(auto_we_builder/Thumbs.db)
(auto_we_builder/coverage/)
(auto_we_builder/htmlcov/)
(auto_we_builder/.coverage)
(auto_we_builder/.mypy_cache/)
(auto_we_builder/.pytest_cache/)
(auto_we_builder/*.zip)
(auto_we_builder/*.gz)
(auto_we_builder/*.tar)
(auto_we_builder/*.tgz)
(auto_we_builder/*.bz2)
(auto_we_builder/*.xz)
(auto_we_builder/*.7z)
(auto_we_builder/*.rar)
(auto_we_builder/*.zst)
(auto_we_builder/*.lz4)
(auto_we_builder/*.lzh)
(auto_we_builder/*.cab)
(auto_we_builder/*.arj)
(auto_we_builder/*.rpm)
(auto_we_builder/*.deb)
(auto_we_builder/*.Z)
(auto_we_builder/*.lz)
(auto_we_builder/*.lzo)